apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthmap-adaptatutor
  namespace: healthmap-adaptatutor
  labels:
    app.kubernetes.io/name: healthmap-adaptatutor
    app.kubernetes.io/part-of: healthmap-adaptatutor
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx

  tls:
    - hosts:
        - adaptatutor.mlthrive.com
      secretName: adaptatutor-mlthrive-tls

    - hosts:
        - api-adaptatutor.mlthrive.com
      secretName: api-adaptatutor-mlthrive-tls

  rules:
    - host: adaptatutor.mlthrive.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthmap-adaptatutor-frontend
                port:
                  number: 80

    - host: api-adaptatutor.mlthrive.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthmap-adaptatutor-backend
                port:
                  number: 8000
